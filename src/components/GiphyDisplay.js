
const GiphyDisplay = ({gif}) => {
    // JSX can only return a single parent but it can have unlimited children
    const loaded = () => {
       return( 
       <div>
            <h1></h1>
            <h2></h2>
        </div>
       );w
    }
}